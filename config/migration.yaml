platform:
  install_dir: /opt/bitrix-migration
source:
  db_dsn: mysql:host=127.0.0.1;dbname=bitrix_source;charset=utf8mb4
target:
  rest_webhook: https://bitrix.example/rest/1/token
workers:
  worker_count: 4
  batch_size: 100
  rate_limits: 25
backup:
  schedule: daily
  retention_days: 30
